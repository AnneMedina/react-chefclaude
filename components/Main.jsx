export default function Main(){
    return (
        <main>
            <input className="ingredient-input" type="text" placeholder="e.g. oregano" />
            <button className="ingredient-add">+ Add ingredient</button>
        </main>
    )
}